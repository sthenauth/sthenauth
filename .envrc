# -*- sh -*-

# For loading run-time assets:
export sthenauth_datadir=$(pwd)/sthenauth
export sthenauth_certauth_datadir=$(pwd)/sthenauth-certauth

# Left over settings for the server that need to be updated:
export STHENAUTH_CONFIG="$(pwd)/sthenauth-server/tmp/config.yml"
export STHENAUTH_SECRETS_DIR="$(pwd)/sthenauth-server/tmp/secrets"
